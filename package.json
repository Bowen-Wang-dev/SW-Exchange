{
  "name": "sw-exchange",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "postinstall": "pnpm --filter @sw-exchange/shared build",
    "dev": "pnpm --filter @sw-exchange/shared build && concurrently -n api,web -c cyan,yellow \"pnpm --filter @sw-exchange/api dev\" \"pnpm --filter @sw-exchange/web dev\"",
    "build": "pnpm --filter @sw-exchange/shared build && pnpm --filter @sw-exchange/api build && pnpm --filter @sw-exchange/web build",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:seed": "pnpm --filter @sw-exchange/shared build && pnpm --filter @sw-exchange/api seed",
    "smoke": "tsx scripts/smoke.ts"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "dotenv": "^16.6.1",
    "drizzle-kit": "^0.31.7",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
